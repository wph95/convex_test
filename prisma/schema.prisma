generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Bookmark {
  id         Int      @id @default(autoincrement())
  repository String
  date       DateTime
}
